# Copy this file to .env (local) or set the same names on the host.
# Never commit .env or put the secret in a PUBLIC_/VITE_ variable.
SUPABASE_URL=
SUPABASE_SECRET_KEY=

# Optional public HTTPS link or root-relative URL. Empty uses the bundled PDF.
DECK_URL=
