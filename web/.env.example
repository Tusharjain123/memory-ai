# For the local Cloudflare-backed preview, copy this file to .dev.vars.
# Never commit .dev.vars or put the secret in a PUBLIC_/VITE_ variable.
SUPABASE_URL=
SUPABASE_SECRET_KEY=

# Optional public HTTPS link or root-relative URL. Empty uses the bundled PDF.
DECK_URL=
